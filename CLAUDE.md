# naverblog — 작업 시작 전 필독

> 이 파일은 어느 PC에서든 레포를 열면 Claude가 자동으로 읽는다. **로컬 메모리(~/.claude)는 PC 간에 옮겨지지 않으므로, 결정·상태는 반드시 레포 파일에 남기고 push한다.**

## 🔴 최상위 규칙
- **`카테고리_포트폴리오.md`** — 어떤 계열을 쓰고 DROP하는지의 단일 기준. 규칙이 서로 어긋나면 이 파일이 이긴다.
- 목표: 애드포스트 **일 5~8천 원 → 10배**. 성과 없는 계열은 과감히 DROP, 성과 있는 계열은 발굴·승격.
- 주제 규칙은 사용자에게 묻지 말고 레포에서 찾는다(사용자 지시: "규칙은 네가 제일 잘 안다").

## PC 간 작업 방식
1. 작업 시작: `git pull`
2. 작업 끝: 바꾼 파일만 골라 commit → `git push`
3. ⚠️ **자동 발행 스케줄러(`NaverblogDaily`, 🟢 매일 04:00 — 2026-09-25 복원)는 한 대의 PC에서만 켠다.** 두 PC에서 켜면 같은 날 원고가 두 번 생성되고 push 충돌이 난다. 다른 PC에서는 분석·규칙 작업만 한다.
4. 스케줄러 PC가 새벽에 push한 원고가 있으니, 낮에 다른 PC에서 작업하기 전 반드시 pull.
5. 🟢 **매일(월~일) 04:00 자동 발행**(2026-09-25 사용자 지시 복원). 요일 구성(토: travel 2, pass 1, car 1, rite 1, house 1, lease 1 / 일: travel 3, pass 1, local 1, ins 1, repair 1) 및 검증 게이트가 동일하게 적용된다.


## ⏸️ 업로드가 밀리면 생성이 자동으로 멈춘다 (2026-09-17 신설)

**왜**: 네이버 업로드는 수동 붙여넣기다. 사용자가 못 올리는 동안에도 매일 7건이 찍혀 나오면
백로그가 눈덩이가 되고 결국 시즌을 넘겨 버려진다(7/1~9/10에 실제로 112건이 그렇게 사라졌다).

**어떻게**: `daily-run.ps1` **Step 0.3**이 매 실행 첫머리에
`https://rss.blog.naver.com/<naverBlogId>.xml` 로 **실제 등록분을 읽어** `발행이력.md`와 대조한다.
미등록이 `uploadPauseThreshold`(기본 7 = 하루치) 이상이면 **그날 생성을 건너뛴다**(exit 4, 예산 소모 없음).

**재개**: 밀린 원고를 네이버에 붙여넣으면 RSS에 잡혀 **다음 실행에서 자동으로 재개**된다.
🔑 **이 PC에 접속하거나 파일을 고칠 필요가 없다.** 여행 중이어도 상관없다.

| 상황 | 동작 |
|:--|:--|
| 설정 `naverBlogId`가 비어 있음 | 기능 꺼짐(그냥 통과) |
| RSS·네트워크 오류 | 경고만 남기고 **통과** — 점검 때문에 발행을 막지 않는다 |
| 오늘 생성분 | 판정 제외(아직 붙여넣기 전일 수 있음). **어제까지만** 본다 |
| RSS 커버(최근 50건) 밖 오래된 날짜 | 판정 제외 |
| 급히 강행 | `.scripts/logs/SKIP_UPLOAD_CHECK` 파일 생성 → 1회 우회(소모됨) |

⚠️ **한계**: 네이버에 올릴 때 제목 **앞부분**을 바꾸면 다른 글로 보여 미등록으로 잡힌다
(뒷부분 수정은 괜찮다 — 앞 14자로 대조). 임계가 7이라 한두 건은 멈추지 않는다.

## 파일 지도
| 파일 | 역할 |
|:--|:--|
| **`정책_현행요약.md`** | 📋 **현행 정책 한 장 요약** — 요일 구성표·배정·임계·소재 규율·판정일. *읽기용 사본(고쳐도 안 바뀜)* |
| `카테고리_포트폴리오.md` | 🚦 계열 상태판 · 판정 기준 · 발굴 대기열 · 투입/DROP 절차 · 주기 루틴 |
| `.scripts/daily-prompt.md` | 월~금 04:00 무인 발행 지시문 — **§2 믹스·요일표·보충·임계의 정식 사양** |
| `.scripts/daily-run.ps1` | 발행 래퍼 (월요일 큐 리필 → 원고 → 검증 게이트 → commit/push) |
| `Naverblog.md` | 원고 운영 지침 — 01 카테고리 구조 · 03 제목(3.3 클릭 설계) · 04 포맷 |
| `생활정보.md` | 비여행 발행 큐 — §01 인접 · §02 rite_/car_/gov_ · §04 pass_/house_ · **§05 🧪 스프린트** |
| `국내여행지.md` | 여행 발행 큐 §02 |
| `발행이력.md` | 전 원고 색인(중복 방지) — 삭제 금지 |
| `stats/weekly/*.json` | 주간 조회수 top-N (스키마 `stats/weekly/SCHEMA.md`) |
| `stats/분석_*_주간.md` | 주간 분석 기록 |
| `stats/kpi_weekly.csv` | 주간 KPI 한 줄씩 |
| `stats/연구_카테고리유니버스_20260915.md` | 36개 주제 점수화 연구 (발굴 근거) |
| `.claude/agents/naver-stats-analyst.md` | 주간 분석 + 포트폴리오 판정 |
| `.claude/agents/queue-refiller.md` | 큐 리필 규칙 (임계표) |

## 사용자가 "stats 업데이트했어"라고 하면
`카테고리_포트폴리오.md §5` 루틴을 그대로 수행한다: 캡처 → JSON 적재 → `naver-stats-analyst` 분석(포트폴리오 판정 포함) → 판정대로 DROP·승격·투입 적용 → `stats/kpi_weekly.csv` → commit·push → 4줄 보고.

## 데이터 한계 (분석 시 항상)
- 주간 통계는 top-N 컷 — 컷 밖 글은 안 보인다. 커버리지가 다른 주를 섞어 평균 내지 말 것.
- 네이버 도메인은 브라우저 자동 조회 차단 — 통계는 사용자 캡처로만 들어온다.
- 애드포스트는 글별 수익을 주지 않는다 — 계열 효과는 CPC 추이로만 본다.
