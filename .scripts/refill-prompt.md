# Naverblog 주간 큐 리필 (매주 월요일 04:00 — daily-run.ps1 Step 0.5)

이번 실행의 임무는 **발행 후보 큐 리필 하나뿐**이다. 원고는 쓰지 않는다.

## 할 일

**`.claude/agents/queue-refiller.md`를 읽고 그 지시를 네가 직접 수행하라.** 오늘 날짜 기준으로 큐를 실사하고 임계 미달 계열만 채운다.

그 파일에 규칙(임계표·시즌게이트·품질 규율·계열별 소재 규율·적재 형식·출력 형식)이 전부 있다. **이 파일에는 규칙을 중복해 적지 않는다.**

> ⚠️ **서브에이전트로 재위임하지 마라**(2026-08-10). 이 실행은 이미 리필 전용 세션이라 보호할 컨텍스트가 없고, 한 겹 더 띄우면 같은 파일을 두 번 읽어 예산만 늘어난다. 실제로 2026-08-10 04:00 실행이 그렇게 `Exceeded USD budget`으로 죽었다.
> 대화 중에 사람이 부를 때는 반대로 **`queue-refiller` 서브에이전트로 호출**하는 것이 맞다(그때는 보호할 대화 컨텍스트가 있다). 같은 규칙 파일을 쓰므로 결과는 동일하다.

## 출력

서브에이전트가 낸 `[REFILL] ...` 줄을 그대로 표준출력에 남겨라(로그에 기록된다). 아무것도 덧붙이지 않는다.

## 금지

- 원고(HTML) 작성·`output/` 수정 — Step 1의 일이다.
- git commit·push — Step 2가 처리한다.
- `daily-prompt.md`·전략 문서 수정, **임계값 변경**.

---

> ⚙️ **2026-08-09 개편**: 리필 규칙 전문(全文)이 이 파일에서 `.claude/agents/queue-refiller.md`로 이동했다.
> **규칙을 고칠 곳은 그 에이전트 파일 하나다** — 두 곳에 같은 규칙을 두면 이 파일이 그랬듯 개정에서 누락된다
> (실제로 이 파일은 8/9 믹스 개정의 §04 3계열이 빠지고 폐지된 `cert_`가 남은 채였다).
> 임계값의 최종 출처는 여전히 `.scripts/daily-prompt.md §2`다.
