# Naverblog 일일 자동 발행 작업

매일 새벽 4시 KST 작업 스케줄러가 실행. 작업 디렉터리는 이미 `D:\lightsail\naverblog`.

**큰 틀**: 네이버 원고 7건(성장모드) → **티스토리 애드센스용 1건(미러 아님·독립 원본)** → 인스타 중단(2026-07-08).

> # 🚦 최상위 규칙 (2026-09-15 신설 — 이 문서의 어떤 규칙보다 우선)
> 1. **어떤 계열을 쓰고 버리는지는 `카테고리_포트폴리오.md` §2 상태판이 결정한다.** 이 문서와 어긋나면 그 파일을 따른다.
> 2. ⛔ **DROP 계열 = `recipe_`·`tech_`·`appli_`·`cert_`** — 큐에 `작성대기중` 행이 남아 있어도 **선정·보충 절대 금지.**
> 3. 🧪 **스프린트 A = `labor_`·`lease_`·`ins_`·`repair_`** (2026-09-16 가동, 소스 `생활정보.md §05`) — 요일표대로 반드시 발행.
> 4. 🚫 **여행으로 보충 금지** — 비여행 칸이 비면 비여행 계열로 채운다(§2 부족 시 보충).
> 5. 🎯 **모든 원고 클릭 설계 필수** (§4 · `Naverblog.md 3.3`) — 클릭 블록 없으면 저장 금지.

---

## 1. 컨텍스트 로드

다음 네 파일을 Read:

1. `Naverblog.md` — 운영 지침 (Platinum 템플릿·티스토리 규칙·팩트체크)
2. `국내여행지.md` — 여행 주제 DB (§02 발행 후보가 선정 대상)
3. `생활정보.md` — 생활정보 주제 DB (§01 인접 롱테일·§02·§04가 선정 대상). §00에 믹스·prefix·YMYL 규칙.
4. **`발행이력.md`** — **지금까지 발행한 전 원고의 발행일·계열·slug·제목 색인(중복 방지 백데이터).**
   `output/<YYMMDD>/`는 매주 월요일 **최근 7일만 남기고 정리**되므로(daily-run Step 0.6), 과거에 무엇을 썼는지는 **이 파일에만 남는다.** 주제를 확정하기 전에 **여기서 제목·지역·대상을 검색해 겹치는지 확인**한다. 겹치면 그 행을 건너뛰고 다음 후보로 내려간다(시즌게이트와 동일 처리).

오늘 날짜를 `Get-Date -Format yyMMdd` 또는 `date +%y%m%d`로 확인해 `YYMMDD` 변수 사용.

> 🔴 **요일은 반드시 명령으로 계산한다 — 추측 금지** (2026-09-15 신설). `python -c "import datetime;print('월화수목금토일'[datetime.date.today().weekday()])"` 결과를 §2 요일표에 쓴다. 계기: 2026-09-15(**화**)을 「월」로 적고 월요일 로테이션으로 발행했다. 대시보드 헤더의 요일도 이 값으로 적는다. 전날 폴더 `output/<yesterday>/`에서 내부 링크 후보 2개 추출(최근 7일은 보존되므로 항상 존재).

---

## 2. 주제 7건 선정 (하이브리드 믹스 — 2026-06-11 도입 · 건수는 2026-07-07 성장모드로 5→7)

먼저 `output/YYMMDD/index.html` 존재 여부 확인:

- **Resume 모드** (대시보드 있음): 기존 행 중 `⏳ 작성 대기`인 것만 작성. `✔️ 작성 완료` 행은 절대 손대지 않는다.
- **Fresh 모드** (대시보드 없음): 아래 **시즌 연동 믹스**로 합계 **7건** 선정. 각 표에서 상태 `작성대기중`인 행을 **위에서부터** 순서대로. `§03 발행 완료 로그`·여행 `§01`은 선정 대상 아님.

**주제 믹스 (🔴 2026-09-16 개정 · 일 7건 유지 · 목표 애드포스트 10배)** — 국면 무관 고정. 계열 상태 = `카테고리_포트폴리오.md`. 근거 `stats/연구_카테고리유니버스_20260915.md`.

| 구성 | 건수 | 소스 |
|:---|:---:|:---|
| 여행 | **2** | `국내여행지.md §02` (2026-09-13: 3 → 2) |
| 인접 롱테일 | **1** | `생활정보.md §01` — **해외·교통 여행실무 우선**(환전·로밍·여권·항공·기차) |
| 생활 버티컬 | **2** | `생활정보.md §02` — **`rite_` 1 + `car_` 1 고정**(검증된 승자 2종만) |
| **로테이션 (pass_ · house_ · 🧪 스프린트)** | **2** | `생활정보.md §04-D`(pass_) · `§04-B`(house_) · **`§05`(🧪 labor_·lease_·ins_·repair_)** — 아래 요일표 |

> ⚙️ **2026-09-13 개정**(사용자 지시). 계기 = 9/11 주간 분석. **계열마다 글의 수명이 다르다**는 실측이 근거다.
> - **3주 생존율**: 경조사 96% · 지원금 45% · 자동차/인접 27% · **국내여행 16%**. 여행이 발행의 45%를 쓰면서 가장 빨리 죽는다.
> - **여행 3 → 2.** 뺀 한 칸은 경조사 증량이 아니라 **신규 계열**에 준다(사용자 판단). 경조사는 이미 42편으로 검증된 자리라 증량 효과가 예측 가능한 소폭인 반면, 미개척 계열은 upside가 크다.
> - **새 칸을 만들지 않고** 기존 §04 로테이션을 1 → 2칸으로 늘렸다. 인큐베이터 절차(4주 후 승격·회수)를 그대로 쓸 수 있고 되돌리기 쉽다.
> - ⚠️ **단기 트래픽은 더 떨어진다.** 신규 계열은 익는 데 2~3주 걸리고 여행 한 칸은 즉시 빠진다. 단기 방어는 ①큐로 되돌린 미업로드 원고 107행 ②추석 클러스터 ③리프레시 루프가 맡는다. 이 개정은 그 위에 얹는 **장기 투자**다.

> ⚙️ **2026-08-09 개정 내역**(사용자 지시). 총량 7 유지. 근거 = 카테고리 분석(휴면 카테고리 중 에버그린 상위군을 깨우되, 검증된 축은 지키는 방향).
> - 생활 버티컬 **3 → 2**: `rite_`·`car_`만 고정 유지하고 **`appli_`/`gov_`/`cert_` 3순위 공유 슬롯은 폐지**(신규 로테이션에 양도).
>   - ~~단 **완전 폐기 아님** — 신규 로테이션 큐가 고갈되면 그 자리를 `appli_`(시즌) → `gov_`(다변화) 순으로 메운다.~~ → **2026-09-13 `appli_` 보충 제외**(실적 0.9뷰/건). `gov_`만 남는다.
> - **신규 버티컬 3계열 신설**: 카테고리 분석에서 월당 상위였으나 발행이 멈춰 있던 계열. 각각 1/일이 아니라 **합쳐서 1/일**(로테이션)로 시작 — 계열당 큐 버퍼 요구를 14 → 8로 낮추기 위함. 4주 후 코호트 4주차 실적으로 승격·회수 판정(버티컬 인큐베이터 방식, `rite_` 승격 선례와 동일).
> - **인접 §01은 해외·교통 실무로 더 기울인다**: 카테고리 분석에서 해외여행이 월당 94(휴면 3위)였고 그 성과가 이 슬롯에서 나왔다. 환전 761·로밍 540·여권 4,212(2018년 글)가 근거.

**생활 버티컬 2건 (2026-08-09 개정 — 3건 → 2건)**: 조회수로 검증된 승자 2종만 고정 배치.
> ① **`rite_`(경조사) 1건 — 매일 상설**(부고 1,261=전체 4위, 2026-07-28 정식 승격). ② **`car_`(자동차 행정) 1건**(과태료·명의이전 등 상위 검색질의 강함). slug는 각 prefix로 통계 분리.
> ~~③ `appli_`→`gov_`→`cert_` 3순위 공유 슬롯~~ → **폐지**(신규 로테이션에 양도). 보충 후보로만 남김.

**로테이션 2건 — 요일 고정 (🔴 2026-09-16 개정: `recipe_`·`tech_` DROP → 🧪 스프린트 A 4계열 투입)**
소스 = `생활정보.md §04`(pass_·house_) + **`§05`(스프린트)**. 계열 상태의 유일한 기준 = **`카테고리_포트폴리오.md`**. 요일로 **결정론적 배정**(랜덤·재량 금지). 하루 **2건**이고, 아래 표의 ①②를 그날 둘 다 쓴다.

> 🔒 **이 표의 기계용 사본이 `.scripts/policy.json`에 있다.** `daily-run.ps1`이 Step 0.7에서 그 파일로 **오늘 요일과 기대 계열을 계산해 이 프롬프트 맨 앞에 주입**하고, Step 1.8에서 같은 표로 발행 결과를 검증한다.
> → **프롬프트 맨 위의 「🔒 오늘의 확정 사항」 블록이 이 표보다 우선한다.** 요일은 절대 직접 계산하지 말고 그 블록의 값을 그대로 쓴다(2026-09-15 요일 오산 → 원고 7건 재작성 사고).
> → 표를 고칠 땐 `policy.json`도 같이 고친다(`카테고리_포트폴리오.md §4` 절차에 명시).

| 요일 | 로테이션 ① | 로테이션 ② |
|:--:|:--|:--|
| 월 | `pass_` | 🧪 `labor_` |
| 화 | 🧪 `repair_` | 🧪 `lease_` |
| 수 | `pass_` | 🧪 `ins_` |
| 목 | `house_` | 🧪 `labor_` |
| 금 | `pass_` | 🧪 `repair_` |
| 토 | 🧪 `lease_` | 🧪 `ins_` |
| 일 | `pass_` | `house_` |

| 계열 | prefix | 주 배정 | 상태 | 소스 |
|:--|:--|:--:|:--|:--|
| 교통 요금·환급 제도 | `pass_` | **4** | 상설 (건당 179.8, 1위) | §04-D |
| 전원주택·텃밭 | `house_` | **2** | ✅ 유지 (4 → 2 축소) | §04-B |
| 🧪 노무·직장 | `labor_` | **2** | 스프린트 A · 첫 판정 10-05주 | §05-A |
| 🧪 임대차·이사 | `lease_` | **2** | 스프린트 A · 첫 판정 10-05주 | §05-B |
| 🧪 보험 청구·실무 | `ins_` | **2** | 스프린트 A · 첫 판정 10-05주 | §05-C |
| 🧪 집수리·결로곰팡이 | `repair_` | **2** | 스프린트 A · 첫 판정 10-05주 | §05-D |
| ~~레시피~~ | ~~`recipe_`~~ | 0 | ⛔ **DROP 2026-09-15** (건당 15.2) | 선정·보충 금지 |
| ~~기기·IT 실무~~ | ~~`tech_`~~ | 0 | ⛔ **DROP 2026-09-15** (건당 14.2) | 선정·보충 금지 |

> 🧪 **스프린트 계열 소재 범위 — 벗어나면 실험 무효** (상세·금지 목록은 `생활정보.md §05` 각 섹션 헤더):
> - `labor_` 노무·직장: 퇴직금·연차수당·주휴수당·퇴사 절차·육아휴직 급여. **계산표·예시 필수.** 고용노동부·고용24·근로복지공단 1차 출처 + `(재검증)`. ⛔ 개별 분쟁 판단.
> - `lease_` 임대차·이사: 전월세 계약 체크리스트·특약 문구·보증금 반환 절차·전세보증보험·중개수수료·이사 비용. ⛔ **매매·시세·투자·대출 권유.**
> - `ins_` 보험 청구·실무: 실손 청구 서류·기한·지급 거절 대응·주택화재보험 배상. ⛔ **자동차보험(car_ 담당)·특정 상품 가입 권유·보장 추천.**
> - `repair_` 집수리: 결로·곰팡이·누수·실리콘·도배·방충망·외풍. **셀프 vs 업체 비용 비교 필수.** ⛔ 구조·전기·가스 셀프 시공 권유.
> - `pass_` 범위: 교통 요금 제도와 그 환급·감면 — 지자체 패스(K-패스·기후동행·더경기패스·I-패스·동백전 등) · 하이패스 파생 · 통행료 감면 · 정기권 · KTX/SRT/고속버스 할인 · 환승. YMYL(1차 출처·`(재검증)`), 헤드키워드 금지("K-패스 신청" ❌ → "K-패스 광역버스도 환급되나" ⭕). 여행 글과 내부링크.
> **그날 요일 계열의 큐가 비면**: 같은 스프린트의 다른 계열 → `pass_` → `car_` → `rite_` 순. **여행으로 보충 금지.** ⛔ `recipe_`·`tech_`·`appli_`는 보충에서도 제외.

> 여행 2건 고정 = 주 14건 수요 → §02 버퍼(여행 ≥18) 유지(G3 플래그 계속). 국면(성수기/평시/비수기)은 건수를 바꾸지 않고, 아래 시즌 게이트·G1/G2가 '어떤' 항목을 고를지에만 적용된다.

**⏱️ 시즌 게이트 (선정 필수 — 2026-06-20 추가 · **2026-09-13 「수요 피크 D-14」로 개정**)**: 각 표에서 `작성대기중`을 위에서부터 고르되, **그 행의 '발행 타이밍'이 오늘 발행일과 맞지 않으면 건너뛰고 다음 행으로 내려간다.**

> 🔴 **2026-09-13 개정 — 창의 뒤쪽을 잘라낸다.** 옛 규칙은 창 **±2주**(창 시작 2주 전 ~ 창 종료 2주 후)였다. 새 규칙은 **창 시작 2주 전 ~ 창 종료**까지만이다.
> **근거**: 글은 발행 후 **2주차에 노출이 정점**이다(`reference_naver_view_maturation`). 따라서 절정 2주 전에 나가야 정점과 검색 수요가 겹친다. 절정이 지난 뒤 발행하면 정점이 수요 소멸 구간에 떨어진다.
> **실측**: 울주 신불산 억새를 8/15에 발행 → 8월 말 676 찍고 9월 초 299로 반토막. 억새 절정은 10월 중순이라 **카드를 6주 일찍 썼다.** 반대로 8/24~26에 나간 선운사 꽃무릇·서산 꽃게·물의정원 코스모스는 절정과 2~3주 간격이라 9월에도 상승했다.

판정 규칙:
- 발행 타이밍에 **특정 월·시즌**(예 `10월 중순`, `9~10월`, `5월`)이 적힌 행은 **[창 시작일 − 14일] ~ [창 종료일]** 안에 들 때만 선정 대상.
  - ✅ 타이밍 `10월 중순` → **9월 말부터** 선정 가능, 10월 말까지.
  - ❌ 타이밍 `10월 중순`을 8월에 선정 → **너무 이르다.** 건너뛴다.
  - ❌ 타이밍 `8월` 행을 9월에 선정 → **이미 지났다.** 건너뛴다(옛 규칙에선 통과했다).
- `상시`·`에버그린`, 그리고 **지금 진행 중인 시즌**은 그대로 선정.
- 건너뛴 행은 상태를 `작성대기중` 그대로 두어 **다음 적기에 재선정**되게 한다. 그 결과 부족해진 건수는 아래 '부족 시 보충' 규칙으로 채운다.
- ⚠️ **이 규칙은 큐 행의 '발행 타이밍'이 「현장 절정 시기」로 정확히 적혀 있어야 작동한다.** 큐를 리필·수정할 때 타이밍 칸에는 **발행하고 싶은 시기가 아니라 실제 절정 시기**를 적는다(억새 = 10월 중순, 단풍 = 10월 말~11월 초, 김장 = 11월 중하순). 2주를 빼는 건 이 게이트가 알아서 한다.

**🧮 결정론적 운영 게이트 (2026-06-27 조회수 분석 기반 — 시즌게이트와 함께 위에서부터 적용. 근거 `stats/운영원칙_조회수기반_20260627.md`)**:
- **G1. 다변화 가드 (키워드 패밀리 상한 6)**: 신규(§02 일반행) 선정 시, 후보 행 제목의 **핵심 제도·키워드 토큰**(첫 고유명사, 예 `K-패스`·`근로장려금`·`에너지바우처`)이 `생활정보.md §02 표`에 이미 `작성완료` **6건 이상**인 패밀리면 그 행을 건너뛴다(시즌게이트와 동일 — 상태 유지·다음 행). 한 키워드 과집중(K-패스 포화) 재발 방지. 인접(§01)·파일럿도 동일 토큰 6건↑이면 동일 적용.
- **G2. 승자형 우선 (동일 적기 타이브레이크)**: 시즌게이트·G1을 통과한 후보가 같은 적기에 복수면, 제목에 **검색 의도어·비교어**(`비교`·`얼마`·`요금`·`무료`·`당일치기`·`가는 법`·`신청 방법`·`BEST`·`어디`)가 있는 행을 위로 끌어올려 우선 선정(없으면 기존 위에서부터). 근거: 비교·큐레이션·질의형이 조회수 최상위(수도권 계곡 당일치기 1,500·여의도 주차 350).

- **여행 2건**: `국내여행지.md §02 발행 후보` 작성대기중 위에서 2건. (2026-09-13: 3 → 2)
- **인접 1건**: `생활정보.md §01 인접 롱테일` 작성대기중 위에서 1건 — **해외·교통 여행실무**(환전·로밍·여권·항공·기차·수하물·보험)를 **최우선**. 지역생활 소재는 그다음. 본문에 관련 여행 원고 내부링크.
- **생활 버티컬 2건**: ① `rite_` 상설 1 → ② `car_` 1. **헤드키워드 금지·YMYL 재검증**(생활정보.md §00) 동일 적용.
- **로테이션 2건**: `생활정보.md §04`·**§05**에서 **오늘 요일에 배정된 두 계열**의 작성대기중 위에서 각 1건씩(위 요일 표 ①②). 예) 월요일 = `pass_` 1 + `labor_` 1.

**부족 시 보충** (🔴 2026-09-16 개정 — 여행 누수 차단):
1. 로테이션 슬롯이 비면 → 같은 스프린트의 다른 계열(§05) → `pass_` → `car_` → `rite_` → `gov_`(교통·환급 소재만).
2. 생활 버티컬(`rite_`·`car_`)이 비면 → §01 해외·교통 실무 → §05 스프린트 → `pass_`.
3. 인접(§01)이 비면 → `pass_` → §05 스프린트.
4. 🚫 **여행 §02는 여행 칸(2건)에만 쓴다.** 비여행 칸을 여행으로 채우는 건 **모든 비여행 큐가 시즌게이트 통과 0건일 때만** 허용하고, 그 경우 완료 출력에 `[LEAK travel N]`을 반드시 남긴다.
> 근거: 2026-09-01~15 발행 105건 중 **여행 43건(41%)**. 보충이 여행으로 새면서 수명 최하 계열(3주 생존 16%)이 다시 비중 1위가 됐다.
> ⛔ `appli_`·`recipe_`·`tech_`·`cert_`는 보충에서도 제외(`카테고리_포트폴리오.md` DROP).
> 🔴 `gov_`로 보충할 땐 교통패스·환급 주제만. 그 주제가 `pass_`와 겹치면 `pass_` slug로 발행(통계 분리).
> ⚠️ 보충으로 **한 계열이 하루에 3건 이상** 되지 않게 한다.

**큐 부족 플래그 (G3 — 완료 출력에 덧붙임. 본 실행 중단 금지·신규 주제 자동 추가 금지)**:

> ⚙️ **2026-08-01 임계 전면 상향 + 주 1회 정례 리필 도입**(사용자 지시). 계기: 8/1 실행에서 여행 외 큐가 **전부 고갈**(시즌게이트 통과 0건) → '부족 시 보충' 규칙이 발동해 **7건 전부 `travel_`** 발행. 옛 임계(여행 15)는 일 7건 소비 대비 버퍼가 2일뿐이라 주 1회 주기를 못 버틴다. **새 임계 = 주간 소비량 + 여유**(일 7건 × 7일 = 주 49건 소비 기준).

| 큐 | 일 소비 | 주 소비 | **임계(미만이면 플래그)** |
|:---|:---:|:---:|:---:|
| 여행 §02 | 2 | 14 | **18** |
| 생활 §01 인접 (해외·교통 실무 우선) | 1 | 7 | **14** |
| `rite_`(상설) | 1 | 7 | **14** |
| `car_` | 1 | 7 | **14** |
| **§04 `pass_`** | (로테이션) | 4 | **12** |
| **§04 `house_`** | (로테이션) | 2 | **6** |
| **§05 🧪 `labor_`·`lease_`·`ins_`·`repair_` 각각** | (로테이션) | 2 | **6** |
| ~~`recipe_`~~·~~`tech_`~~ | ⛔ DROP 09-15 | — | — |
| `gov_` (보충 후보) | — | — | **5** |
| ~~`appli_`~~·~~`cert_`~~ | 폐지 | — | — |

> 📌 **§04 임계 근거**: 약 3주 버퍼 기준. 주 4건 계열은 12, 주 3건 계열은 10. 각 1/일(주 7건)로 승격하면 임계를 **14로 올린다.**
> 📌 **여행 25 → 18** (2026-09-13): 주 소비가 21 → 14로 줄었으므로 버퍼도 같은 비율로 내린다.

- 여행 §02 작성대기중 < 18 → `[QUEUE LOW travel: N]`
- 생활 §01 작성대기중 < 14 → `[QUEUE LOW local: N]`
- `rite_` < 14 → `[QUEUE LOW vertical:rite_ N]` (**상설 슬롯 — 비면 매일 1건이 즉시 여행으로 새어나감**)
- `car_` < 14 → `[QUEUE LOW vertical:car_ N]` (회고는 발행 **3주차 성숙 후 동일발행일 상대비교**)
- §04 `pass_` < 12 → `[QUEUE LOW vertical:pass_ N]`
- §04 `house_` · §05 `labor_`·`lease_`·`ins_`·`repair_` **각각** < 6 → `[QUEUE LOW vertical:<slug> N]`
- `gov_`(보충 후보) < 5 → `[QUEUE LOW vertical:gov_ N]`
  > ⚙️ 2026-08-10 정정: 위 임계표와 이 목록이 어긋나 있었다 — gov_가 목록에만 `<10`으로 남고, 폐지된 `cert_`가 남고, §04 계열이 빠져 있었다. **임계표가 기준이다.** ~~`cert_`~~·~~`appli_`~~는 폐지(각 2026-08-09·2026-09-13)라 플래그 대상이 아니다.
- ⚠️ **시즌 아닌 대기 행은 버퍼로 치지 않는다**: 임계 계산 시 **오늘 시즌게이트를 통과하는 행만** 센다. 9~10월 행 6건이 남아 있어도 8월엔 0건이다(8/1 고갈의 직접 원인).

**주 1회 정례 리필**: 매주 **월요일** 04:00 실행분에서 `daily-run.ps1` **Step 0.5**가 `refill-prompt.md`를 먼저 돌려 임계 미달 큐를 채운다. 본 프롬프트(daily-prompt)는 **여전히 신규 주제를 스스로 추가하지 않는다** — 플래그만 남기고 발행에 집중.

---

## 3. 대시보드 (`output/YYMMDD/index.html`)

- **Fresh**: 직전일 `output/<yesterday>/index.html` 구조를 답습해 5개 행을 일단 `⏳ 작성 대기` 상태로 생성.
- **Resume**: 신규 생성 X, 기존 그대로.

작성 완료 후 각 행 상태를 `✔️ 작성 완료`로 바꾸고 제목을 원고 파일 상대경로 `<a>`로 감싼다.

---

## 4. 네이버 원고 7건 (`output/YYMMDD/<slug>.html`)

- **템플릿(고정)**: `templates/naver_v5.html`을 **매번 읽어서 그대로 복사**한다. `<style>`·`<script>` 블록은 카테고리 컬러 토큰(`{{ACCENT}}` 등, 표는 템플릿 주석)만 치환하고 **한 글자도 새로 디자인하지 않는다**. 새 CSS 클래스 추가·레이아웃 변경·래퍼 div(`.wrap` 등) 금지, `<body>` 직속 구조 유지, h2 번호는 `01 | 소제목`. (2026-08-16 지시: 매일 디자인이 바뀌어 사용자가 적응 못 하는 문제 차단. 기준 예시 = `output/260815/`·`output/260816/`)
- **슬러그 prefix** (생활정보.md §00): 여행 `travel_*` / 인접 롱테일 `local_*` / 신규도메인 `gov_*`(지원금)·`money_*`(금융)·`home_*`(생활가전)·`admin_*`(행정발급 파일럿 1호)·`rite_*`(경조사 파일럿 2호) / 🚀성장모드 Wave 1 `car_*`(자동차 조회·절차)·`appli_*`(계절가전 관리)·`cert_*`(증명서 셀프발급) / §04 로테이션 `recipe_*`·`house_*`·`tech_*`·**`pass_*`(교통 요금·환급 제도 — 2026-09-13 신설)** / 🧪 **§05 스프린트 `labor_*`·`lease_*`·`ins_*`·`repair_*`(2026-09-16)** / ⛔ `recipe_*`·`tech_*` 신규 금지(09-15 DROP). 여행과 분리해 통계 구분.
- **분량**: 본문(태그·공백 제외) 약 **7,500~8,000자**, h2 꼭지 **9개 이상**, 각 꼭지 12~16행.
- **문장**: 존대말(`~합니다`/`~하세요`), 마크다운 `**` 금지(`<strong>` 사용), 문단 사이 `<br><br>` 강제.
- **제목** (`Naverblog.md` 03조 3.1·3.2): 클리셰(`완전 가이드`·`완벽 가이드`·`총정리`·`끝판왕`·`A to Z`) 금지. `2026`은 검색어 핵심일 때만(같은 날 7건 중 최대 1~2건). 자연스러운 한 문장.
  - **롱테일 엔지니어링(3.2) 전 카테고리 필수**: 제목은 카테고리 라벨이 아니라 *검색 질의문*. `[구체 고유명사·정확 제도명] + [한정 수식어 1~2: 대상·상황·시기] + [검색 의도어/숫자: 요금·얼마·무료시간·신청 방법·가는 법·개화 등]`을 최소 2레버 조합. ❌`서울 4대궁 입장료` → ✅`경복궁 무료입장 되는 날과 4대궁 야간개장`.
- **가격 표기** (Naverblog.md 2.2): 품목별 단가 나열 지양, 공식 고정 요금만(입장료·주차료 등). 먹거리는 "1만 원 안팎" 식 대략 범위.
- **이미지**: **원고 카테고리별 이미지 영역 수 — 여행 원고(`travel_*`)는 3개, 생활정보 원고(그 외 전 prefix: `local_*`·`gov_*`·`money_*`·`home_*`·`admin_*`·`rite_*`·`car_*`·`appli_*`·`cert_*`·`recipe_*`·`house_*`·`tech_*`·`pass_*`·`labor_*`·`lease_*`·`ins_*`·`repair_*`)는 2개** (2026-07-13 발행분부터 적용). 실제 이미지를 생성·삽입할 때(기본)는 아래 **실제 이미지 블록**을, 쿼터 소진 등으로 이미지를 못 만들 때만 **placeholder 블록**을 쓴다. **두 경우 모두 이미지 바로 아래 `<div class="img-caption">` 캡션 줄이 반드시 있어야 한다(생략 절대 금지).**

  **① 실제 이미지 블록 (기본 — `<img>` + 캡션, 캡션 줄 누락 금지):**

  ```html
  <div class="img-area">
  <img alt="[한글 캡션] / AI 제작 이미지" src="../../images/YYMMDD/<seq>_<slug>_<idx>.png" style="width:100%; border-radius:8px; display:block; margin:0 auto;"/>
  <div class="img-caption">[한글 캡션] / AI 제작 이미지</div>
  </div>
  ```

  **② placeholder 블록 (이미지 미생성 시에만):**

  ```html
  <div class="img-area">
    <div class="img-placeholder">
      <div class="ph-head">
        <span>📷 AI 이미지 생성 영역</span>
        <span class="ph-file">images/YYMMDD/<seq>_<slug>_<idx>.png</span>
      </div>
      <pre class="prompt-text" id="prompt-<seq>-<idx>">[영문 프롬프트 80~150 단어, photorealistic, ultra high detail, 1:1 aspect ratio]</pre>
      <button class="copy-btn" onclick="copyPrompt('prompt-<seq>-<idx>')">📋 프롬프트 복사하기</button>
    </div>
    <div class="img-caption">[한글 캡션] / AI 제작 이미지</div>
  </div>
  ```
  - 파일명 강제 `{원고seq:1~7}_{keyword}_{이미지seq:1~3}.png` (정규식 `^images/\d{6}/[1-7]_.+_[1-3]\.png$`). 저장 직전 모든 `<img src>`·placeholder의 `ph-file`이 이 형식인지 확인.
  - **저장 직전 캡션 자가검증 (필수)**: 각 `<img>` 또는 `.img-placeholder` 바로 다음 형제로 `<div class="img-caption">…/ AI 제작 이미지</div>`가 **원고 카테고리별 목표 개수(여행 `travel_*` = 3개, 생활정보 그 외 prefix = 2개)** 만큼 존재하는지 확인. `<img>` 개수 = `img-caption` div 개수 = (해당 카테고리 목표 개수)가 아니면 저장 금지·보완. (캡션 문구는 `<img alt>`와 같아도 무방하나 화면에 보이는 `img-caption` div를 절대 빠뜨리지 않는다.)

- **팩트체크**: 일정·요금·운영시간 등은 `WebSearch`로 2026년 값 확인. **음력 명절(단오·추석·설·정월대보름·초파일·한식 등)은 양력 환산을 반드시 WebSearch로 검증** (예: 2026 단오 = 6/19). 확인 못 하면 구체 날짜 단정 금지(시즌 표기로 회피).
  - **생활정보 §02(지원금·금융) 글 YMYL**: 정부24·복지로·홈택스·건강보험공단 등 **1차 출처를 인용**하고 금액·지급일·자격 요건을 발행 직전 재검증. 확인 못 한 수치는 단정 금지(범위·"공고 확인" 안내로 회피).
- 🎯🔴 **클릭 설계 (2026-09-16 신설 · 전 계열 필수 · `Naverblog.md 3.3`)**: AI 브리핑이 한 줄 답을 가져가도 **들어와야만 얻는 것**을 둔다.
  ① **첫 문단 2~3문장에 핵심 답**(AI 인용 유지 — 메이트 점수)
  ② 본문에 **클릭 블록 1개 이상 필수**: 계산표(금액·기간 예시) · A vs B 비교표 · 서류/준비물 체크리스트 · 그대로 복사해 쓰는 문구 예시 · 단계별 절차표
  ③ 제목에 그 블록이 드러나는 말(`비교`·`계산`·`얼마부터`·`서류`·`체크리스트`·`문구`·`vs`)
  ④ **저장 직전 자가검증: 클릭 블록이 없으면 저장 금지·보완.** 「언제·얼마」 한 줄로 끝나는 단일 사실 질의만으로 된 제목 금지.
- **내부 링크**: 최하단 `recommend-area`에 전날 원고 2건(무작위)을 상대경로 링크. 단 생활정보 글은 주제와 연관된 기존 여행 원고가 전날 폴더에 있으면 그쪽을 우선(브리지 내부링크).

---

## 5. 티스토리 원고 1건 (애드센스 승인용 — `output_tistory/YYMMDD/<slug>.html`)

> ⛔ 티스토리는 **글을 새로 쓰는 일**이다. 네이버 HTML을 CSS만 바꿔 옮기는 것이 아니다. 제목·본문이 네이버와 그대로 같으면 중복 콘텐츠 저품질 위험.

> 🎯 **2026-07-09 재설계 — 티스토리는 사용자 실사진·실경험 기반 '별도' 애드센스 채널**. 미러·날조 AI글이 "가치 없는 콘텐츠"로 반려됨 → 네이버 파생 폐기. **티스토리는 4시 자동 실행에서 생성하지 않는다**(daily-run `$TistorySuspended=$true`로 생성·게이트·스테이징 스킵). 대신 **사용자의 진짜 사진 + 진짜 경험을 소재로 하루 1건, 대화형으로 별도 제작**. 제작 워크플로·표준 = `티스토리_애드센스_제작.md`. 아래 5.2~5.4 원칙은 그 제작에도 그대로 적용(독립 원본·정직·신뢰). ⛔ **이 §5는 자동 실행에서는 실행되지 않는다**(참고용).
>
> **5.1 소재 (네이버와 완전 분리)**: 네이버 자동 원고에서 파생하지 않는다. **사용자가 제공한 실제 사진 + 실제 경험 메모**(다녀온 곳·시기·한 일·관찰·팁·비용)를 소재로, 그 주제의 웹에서 가장 유용한 **진짜 후기/가이드**를 만든다. 진짜 1인칭 경험은 이제 **최고 자산(권장)** — 날조가 아니므로. 정보성 주제면 검증 사실 + 1차 출처로 보강.
>
> **5.2 미러 금지 → 독립 최고 원본**: 네이버 원고를 paraphrase 하지 않는다. 같은 주제로 **처음부터 더 깊고 완결적으로** 쓰고, 네이버판에 없는 **부가가치**를 반드시 넣는다(비교표·단계별 절차·상황별 분기·체크리스트·FAQ·흔한 실수·최신 기준일). 검색자가 다른 데 안 가도 되게.
>
> **5.3 ⛔ 날조 경험 삭제 (핵심 변경)**: "✍️ 직접 가보니" 경험 박스·"평일 오전에 들렀더니" 식 **미체험 1인칭 서술 전면 폐기**. 안 가본 곳·안 해본 절차를 지어내면 구글이 기만적·저품질로 본다. 대신 **정직한 가치**로 신뢰를 만든다 — 검증 사실 + **1차 출처 명시 인용**(정부24·공단·공식 홈) + 최신 기준일 + 솔직한 장단점·한계. "직접 해봤다"가 아니라 "공식 자료를 종합·확인해 정리했다". (실제 개인 경험을 사용자가 보강해 넣는 것은 최고의 자산 — 날조만 금지.)
>
> **5.4 신뢰·UX (정직하게)**: 글 끝 **📝 정보 메모** = 정보 기준일(YYYY-MM-DD) + 인용한 1차 출처 + "요금·일정은 변동되니 공식 채널 재확인". 깔끔한 구조(번호 h2·표·목록)·읽기 쉬운 문단(`<br><br>`)·광고밭 느낌 금지.
>
> 📝 정보 메모 표준 블록:
> ```html
> <div style="background:#eef5f4;border:1px dashed #80cbc4;padding:18px 20px;margin:30px 0;border-radius:8px;font-size:0.95em;color:#444;"><strong>📝 정보 메모</strong><br><br>정보 기준일 YYYY-MM-DD · 출처: [1차 출처명·링크] · 요금·일정은 변동될 수 있으니 공식 채널에서 재확인하세요.</div>
> ```

- **폴더·파일명**: 네이버와 동일 slug.
- **제목** (필수): 네이버 제목과 글자 그대로 같으면 FAIL. 앵글 전환·키워드 재배치·수식어 교체로 단어 일치율 ≤ 60%.
- **본문** (필수): 네이버 원고의 paraphrase가 아니라 **독립 원본**. 동일 팩트(시간·가격·주소·날짜·전화)는 정확히 유지하되, 구성·논지·부가정보(표·FAQ·체크리스트·비교)는 네이버판보다 **확장된 자체 콘텐츠**로. 네이버와 문장 유사도 낮게(게이트 dedup 통과).
- **분량**: 완결성 우선(얇으면 애드센스 반려). 최소 네이버판 이상, **실질 정보로** 채운다(억지 늘리기 금지). h2 9+.
- **출력 형식**: **인라인 `style="..."` 전용**. `<style>` 블록·`class=` 속성·`<script>` 절대 금지(티스토리 에디터가 스트립). 외곽 래퍼 div 강제:
  ```html
  <div style="font-family: 'Apple SD Gothic Neo', sans-serif; font-size: 16px; line-height: 1.9; color: #222; max-width: 780px; margin: 0 auto; padding: 16px;">
  ```
- **표 행 배경**: 각 `<td>`에 직접 `background: #f1faf9;`(여행 짝수행) / `#fff`(홀수행). `nth-child` 안 됨.
- **이미지 (핵심·반복 사고 지점)**: 네이버에서 만든 이미지(`images/YYMMDD/<seq>_<slug>_<idx>.png`)를 **표준 `<img>` 1줄 블록 3개로 재사용한다.** 새 이미지 안 만들고, **네이버의 `img-placeholder` 박스(`📷 AI 이미지 생성 영역`·`📋 프롬프트 복사하기` 등)는 절대 carry over하지 않는다.** 표준 블록:
  ```html
  <div style="margin:30px 0;text-align:center;"><img alt="캡션" src="../../images/YYMMDD/<seq>_<slug>_<idx>.png" style="max-width:100%;height:auto;border-radius:8px;"/><div style="font-size:0.9em;color:#666;margin-top:10px;font-style:italic;">캡션 / AI 제작 이미지</div></div>
  ```
  - `<img>`는 **네이버판과 1:1**(카테고리별 개수 동일 — 여행 `travel_*` 3개, 생활정보 그 외 prefix 2개). 같은 `src` 중복 금지.
  - `alt`·캡션은 네이버와 다른 문구로 재작성(라벨 `/ AI 제작 이미지`는 유지).
  - **⚠️ 캡션은 같은 이미지를 가리켜야 한다 (피사체 고정·문구만 변경)**: 이미지는 네이버와 **완전히 같은 파일**을 공유하므로, 티스토리의 `_<idx>.png` 캡션은 **네이버의 같은 `_<idx>.png` 캡션이 묘사한 피사체·장면을 그대로 유지**한 채 표현(어휘·어순)만 바꿔 쓴다. 예: 네이버 `_2.png`=`청량사 유리보전과 6월 수국` → 티스토리 `_2.png`=`청량사 유리보전 앞 수국`(○). 같은 `_2.png`인데 `강구항 어시장` 같이 **다른 대상**을 적으면 위반(엉뚱한 캡션). 티스토리는 본문 꼭지 순서가 네이버와 달라도 **캡션은 본문 문맥이 아니라 그 이미지가 실제로 보여주는 대상**을 기준으로 쓴다(논리 정합 필수). 작성 후 3개 캡션이 각 src의 실제 피사체와 맞는지 네이버 캡션과 1:1 대조 확인.

- **내부 링크**: 하루 1건이므로 `recommend-area`는 **최근 티스토리 원고 2건**(`../<최근 발행일자>/`의 서로 다른 날, 가능하면 관련 주제)으로 건다. 과거 폴더에 티스토리가 없으면 링크 없이 박스만 두지 말고 가장 근접 2건을 찾는다.
- **해시태그**: 본문 끝에 태그 칩 **10개** (네이버 원본 `.tag` 그대로 또는 동등):
  ```html
  <span style="display:inline-block; background:#f0f0f0; padding:3px 10px; border-radius:5px; margin:3px; font-size:0.85em; color:#666; border:1px solid #ddd;">#키워드</span>
  ```
- **대시보드**: `output_tistory/YYMMDD/index.html` — `<title>`·`<h1>` 글자가 네이버 대시보드와 그대로 같으면 안 된다.

**저장 직전 자가검증** (티스토리 1건):
- [ ] 외곽 래퍼 1개, Platinum h1 1개, 번호 h2 9+, `<style>·class=` 0
- [ ] `<img>` 3개·같은 src 중복 0·placeholder 박스 0, 추천 박스 1개, 태그 칩 10개
- [ ] **미러 아님**: 네이버판에 없는 부가가치 ≥2(표·FAQ·체크리스트·비교 중) 포함, 문장 독립 작성·네이버와 저유사
- [ ] **날조 경험 0**: '직접 가보니·가봤·먹어보니·들렀더니' 등 미체험 1인칭 경험 서술 전무
- [ ] 1차 출처 ≥2 명시 인용 + 정보 기준일 + 공식 재확인 고지(📝 정보 메모)

> 게이트(`.scripts/tistory-gate.ps1`)가 push 직전 자동 검수한다. FAIL이면 wrapper가 commit/push를 차단한다. 위 자가검증을 통과시키면 게이트도 자동 통과.

---

## 6. 인스타 카드 5건

`insta-card-builder` 에이전트가 별도 단계(`daily-run.ps1` Step 1.6)에서 호출된다. 본 프롬프트 단계에선 작성하지 않음.

> ⛔ **2026-07-08 인스타 자동생성 중단** (`daily-run.ps1` `$InstaSuspended = $true`). 블로그 성장모드(일 7건) 전환하며 유료 이미지 예산 회수. 재개는 사용자 지시로 플래그 `$false`.

---

## 7. 트래커 갱신

본 실행에서 작성한 주제만 **출처 표**에서 상태 `작성대기중` → `작성완료(MM.DD)`로 변경: 여행은 `국내여행지.md §02`, 인접 롱테일은 `생활정보.md §01`, 신규도메인은 `생활정보.md §02`, 로테이션은 `생활정보.md §04`·**§05(🧪 스프린트)**. 다른 행은 절대 손대지 않는다. `sub_topic_tracker.md`·`spreadsheet.md`·`receipt.md`는 동결 — 일체 수정 금지.

---

## 8. 금지

- **git 명령 일체** (`add`/`commit`/`push`) — wrapper(`daily-run.ps1`)가 수행
- 네이버 원고에 `<img>` 실삽입 (placeholder만)
- 인스타 이미지 생성 시도 (별도 단계 담당)
- 사용자 확인 요청 (무인 실행)
- 메모리 저장

---

## 9. 완료 출력

성공:
```
[DAILY OK YYYY-MM-DD] 5 naver + 5 tistory created in output/YYMMDD/, trackers updated, 0 git commits
```

오류:
```
[DAILY FAIL YYYY-MM-DD] <단계>: <에러 요약>
```
